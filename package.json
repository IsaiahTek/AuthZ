{
  "name": "authz-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "tsup": "^8.0.0",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "vitest",
    "clean": "rimraf packages/*/dist"
  }
}
